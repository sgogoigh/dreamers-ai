# --- core / data pipeline ---
pandas
python-dotenv
google-generativeai       # legacy SDK used by scripts/script.py (metadata labeling)
google-genai              # new SDK used by scripts/script.py (metadata labeling)

# --- trailer pipeline (Gemini 3.5 Flash + Veo 3.1) ---
google-genai>=1.0.0
pydantic>=2.0
imageio>=2.34
imageio-ffmpeg            # bundles an ffmpeg binary for stitching
av                        # pyav backend for last-frame extraction
pillow

# --- model eval / adapter (GPU; install on Colab) ---
transformers>=4.45
huggingface_hub
# torch, peft, accelerate, bitsandbytes  -> install in the GPU env (see scripts/evaluate_adapter.ipynb)
